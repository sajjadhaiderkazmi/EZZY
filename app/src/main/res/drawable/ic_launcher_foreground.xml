<?xml version="1.0" encoding="utf-8"?>
<!--
  The bolt drawn straight in the 108 viewport rather than scaled up from a 24dp glyph, so it
  cannot come out stretched. It spans x 39..69 and y 26..82 — centred on (54,54) and entirely
  inside the 66dp safe zone, so no launcher mask can clip a point off, and the margin around it
  is even on all four sides.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M60,26L39,57L52,57L48,82L69,51L56,51Z" />
</vector>
